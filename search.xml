<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Go语言工程实践</title>
    <url>/2023/05/19/Go%E8%AF%AD%E8%A8%80%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/</url>
    <content><![CDATA[<h1 id="Go语言工程实践"><a href="#Go语言工程实践" class="headerlink" title="Go语言工程实践"></a>Go语言工程实践</h1><h2 id="Go语言进阶与依赖管理"><a href="#Go语言进阶与依赖管理" class="headerlink" title="Go语言进阶与依赖管理"></a>Go语言进阶与依赖管理</h2><h3 id="01-语言进阶"><a href="#01-语言进阶" class="headerlink" title="01 语言进阶"></a>01 语言进阶</h3><h4 id="并发vs并行"><a href="#并发vs并行" class="headerlink" title="并发vs并行"></a>并发vs并行</h4><ol>
<li>并发：多线程程序在一个核的CPU上运行</li>
<li>并行：多线程程序在多个核的CPU上运行</li>
</ol>
<ul>
<li>Go 可以充分发挥多核优势，高效运行</li>
</ul>
<h4 id="1-1-Goroutine"><a href="#1-1-Goroutine" class="headerlink" title="1.1 Goroutine"></a>1.1 Goroutine</h4><ul>
<li><strong>协程</strong>：用户态，轻量级线程，栈 KB 级别</li>
<li><strong>线程</strong>：内核态，线程跑多个协程，栈 MB 级别</li>
</ul>
<img src="../images/Go语言工程实践/协程-线程-16845038659665.png" style="zoom: 50%;" />

<ul>
<li>在Go语言中，为了创建协程，只需要在调用的函数前面<strong>添加 <code>go</code> 关键字</strong>即可。</li>
</ul>
<h4 id="1-2-CSP"><a href="#1-2-CSP" class="headerlink" title="1.2 CSP"></a>1.2 CSP</h4><ul>
<li><p>CSP（Communicating Sequential Processes）</p>
<p>Go 提倡通过通信共享内存而不是通过共享内存而实现通信。</p>
<img src="../images/Go语言工程实践/CSP-16845038659688.png" style="zoom: 67%;" /></li>
</ul>
<h4 id="1-3-Channel"><a href="#1-3-Channel" class="headerlink" title="1.3 Channel"></a>1.3 Channel</h4><ul>
<li><p>通过 <code>make(chan 元素类型, [缓冲大小])</code> 创建</p>
<ul>
<li>无缓冲通道：<code>make(chan int)</code> </li>
<li>有缓冲通道：<code>make(chan int, 2)</code></li>
</ul>
<img src="../images/Go语言工程实践/channel-16845038659621.png" style="zoom:67%;" /></li>
</ul>
<h4 id="1-4-并发安全-Lock"><a href="#1-4-并发安全-Lock" class="headerlink" title="1.4 并发安全 Lock"></a>1.4 并发安全 Lock</h4><ul>
<li>为了保证协程并发的安全进行，需要进行加锁操作。</li>
</ul>
<h4 id="1-5-WaitGroup"><a href="#1-5-WaitGroup" class="headerlink" title="1.5 WaitGroup"></a>1.5 WaitGroup</h4><ul>
<li><p>用 <code>WaitGroup</code> 来优雅地实现并发进程的同步。</p>
<p><code>WaitGroup</code> 包含有一个计数器：</p>
<ol>
<li>开启协程，计数器 +1；</li>
<li>协程执行结束，计数器 -1；</li>
<li>主协程会阻塞直到计数器为 0（即计数器为 0 表示所有子协程执行完毕）。</li>
</ol>
</li>
</ul>
<h3 id="02-依赖管理"><a href="#02-依赖管理" class="headerlink" title="02 依赖管理"></a>02 依赖管理</h3><h4 id="2-1-Go依赖管理演进"><a href="#2-1-Go依赖管理演进" class="headerlink" title="2.1 Go依赖管理演进"></a>2.1 Go依赖管理演进</h4><ul>
<li>GOPATH -&gt; Go Vendor -&gt; Go Module<ul>
<li>不同环境（项目）依赖的版本不同</li>
<li>控制依赖库的版本</li>
</ul>
</li>
</ul>
<h5 id="2-1-1-GOPATH"><a href="#2-1-1-GOPATH" class="headerlink" title="2.1.1 GOPATH"></a>2.1.1 GOPATH</h5><ul>
<li><p>环境变量 <code>$GOPATH</code> ：</p>
<img src="../images/Go语言工程实践/GOPATH-16845038659622.png" style="zoom: 67%;" />

<p>GOPATH 问题在于无法实现 package 的多版本控制。</p>
</li>
</ul>
<h5 id="2-1-2-Go-Vendor"><a href="#2-1-2-Go-Vendor" class="headerlink" title="2.1.2 Go Vendor"></a>2.1.2 Go Vendor</h5><ul>
<li><p>项目目录下增加 vendor 文件，所有依赖包副本形式放在 <code>$ProjectRoot/vendor</code> </p>
<p>通过每个项目引入一份依赖的副本，解决了多个项目需要同一个 package 依赖的冲突问题。</p>
</li>
<li><p>依赖寻址方式：vendor &#x3D;&gt; GOPATH（先 vendor，vendor 没有再 GOPATH）</p>
</li>
</ul>
<h5 id="2-1-3-Go-Module"><a href="#2-1-3-Go-Module" class="headerlink" title="2.1.3 Go Module"></a>2.1.3 Go Module</h5><ul>
<li>通过 <code>go.mod</code> 文件管理依赖包版本。通过 <code>go get/go mod</code> 指令工具管理依赖包。</li>
</ul>
<h4 id="2-2-Go-Module依赖管理三要素"><a href="#2-2-Go-Module依赖管理三要素" class="headerlink" title="2.2 Go Module依赖管理三要素"></a>2.2 Go Module依赖管理三要素</h4><ol>
<li>配置文件，描述依赖：<code>go.mod</code></li>
<li>中心仓库管理依赖库：<code>Proxy</code> </li>
<li>本地工具：<code>go get/go mod</code></li>
</ol>
<h5 id="2-2-1-依赖配置-go-mod"><a href="#2-2-1-依赖配置-go-mod" class="headerlink" title="2.2.1 依赖配置-go.mod"></a>2.2.1 依赖配置-go.mod</h5><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">module github.com/Moonlight-Zhao/<span class="keyword">go</span>-project-example		<span class="comment">//依赖管理基本单元</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">go</span> <span class="number">1.16</span>		<span class="comment">//原生库</span></span><br><span class="line"></span><br><span class="line">require (	<span class="comment">//单元依赖</span></span><br><span class="line">	github.com/gin-contrib/sse v0<span class="number">.1</span><span class="number">.0</span> <span class="comment">// indirect</span></span><br><span class="line">	github.com/gin-gonic/gin v1<span class="number">.3</span><span class="number">.0</span> <span class="comment">// indirect</span></span><br><span class="line">	github.com/<span class="keyword">go</span>-playground/validator/v10 v10<span class="number">.10</span><span class="number">.0</span> <span class="comment">// indirect</span></span><br><span class="line">	github.com/goccy/<span class="keyword">go</span>-json v0<span class="number">.9</span><span class="number">.6</span> <span class="comment">// indirect</span></span><br><span class="line">    ...</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<ul>
<li>单元依赖标识：<code>[Module Path][Version/Pseudo-version]</code></li>
</ul>
<h5 id="2-2-2-依赖配置-version"><a href="#2-2-2-依赖配置-version" class="headerlink" title="2.2.2 依赖配置-version"></a>2.2.2 依赖配置-version</h5><ul>
<li>语义化版本：<code>$&#123;MAJOR&#125;.$&#123;MINOR&#125;.$&#123;PATCH&#125;</code> <ul>
<li>不同的 <code>MAJOR</code> 之间的代码是隔离的</li>
<li><code>MINOR</code> 用于新增函数等修改</li>
<li><code>PATCH</code> 用于补丁，代码bug的修复</li>
</ul>
</li>
</ul>
<h5 id="2-2-3-依赖配置-indirect"><a href="#2-2-3-依赖配置-indirect" class="headerlink" title="2.2.3 依赖配置-indirect"></a>2.2.3 依赖配置-indirect</h5><ul>
<li>A-&gt;B-&gt;C：<ul>
<li>A-&gt;B 直接依赖</li>
<li><strong>A-&gt;C 间接依赖（indirect）</strong></li>
</ul>
</li>
</ul>
<h5 id="2-2-4-依赖分发-回源"><a href="#2-2-4-依赖分发-回源" class="headerlink" title="2.2.4 依赖分发-回源"></a>2.2.4 依赖分发-回源</h5><ul>
<li>如果将依赖包托管在第三方平台上，会有几个问题：<ol>
<li>无法保证构建稳定性：增加&#x2F;修改&#x2F;删除软件版本</li>
<li>无法保证依赖可用性：删除软件</li>
<li>增加第三方压力：代码托管平台负载问题</li>
</ol>
</li>
</ul>
<h5 id="2-2-5依赖分发-Proxy"><a href="#2-2-5依赖分发-Proxy" class="headerlink" title="2.2.5依赖分发-Proxy"></a>2.2.5依赖分发-Proxy</h5><ul>
<li>为了解决 <code>2.2.4</code> 提出的问题，要用到 Proxy。<ul>
<li>Proxy 会<strong>缓存依赖库</strong>，实现稳定、可靠的分发。</li>
<li>可以将 Proxy 看作是依赖源和开发平台之间的一个“中间商”，该“中间商”会<strong>存储依赖源的各种版本库</strong>，以防在依赖源发生改变后，开发平台无法使用最初依赖的版本库的问题。</li>
</ul>
</li>
</ul>
<h2 id="Go语言工程实践之测试"><a href="#Go语言工程实践之测试" class="headerlink" title="Go语言工程实践之测试"></a>Go语言工程实践之测试</h2><h3 id="03-测试"><a href="#03-测试" class="headerlink" title="03 测试"></a>03 测试</h3><ul>
<li><p>测试是应用发布上线前的最后一步，覆盖率高的测试可以保证上线的应用的可靠性、稳定性和安全性，是避免事故的最后一道屏障。</p>
</li>
<li><p>测试主要分为三种：</p>
<ul>
<li><p>回归测试：QA人工测试</p>
</li>
<li><p>集成测试：对系统功能进行测试</p>
</li>
<li><p><strong>单元测试</strong>：开发者对单独的模块进行测试。 -&gt; 保证质量、提升效率</p>
<img src="../images/Go语言工程实践/单元测试-16845038659643.png" style="zoom: 50%;" /></li>
</ul>
</li>
</ul>
<h4 id="3-1-单元测试"><a href="#3-1-单元测试" class="headerlink" title="3.1 单元测试"></a>3.1 单元测试</h4><h5 id="3-1-1-规则"><a href="#3-1-1-规则" class="headerlink" title="3.1.1 规则"></a>3.1.1 规则</h5><ul>
<li><p>所有测试文件以 <code>_test.go</code> 结尾</p>
</li>
<li><p>测试函数命名规范为：<code>func TestXxx(t *testing.T) &#123;&#125;</code> </p>
</li>
<li><p>初始化逻辑放在 <code>TestMain</code> 中：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">TestMain</span><span class="params">(m *testing.M)</span></span> &#123;</span><br><span class="line">    <span class="comment">//测试前：数据装载、配置初始化等前置工作</span></span><br><span class="line">    </span><br><span class="line">    code := m.Run()</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//测试后：释放资源等收尾工作</span></span><br><span class="line">    </span><br><span class="line">    os.Exit(code)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="3-1-2-例子"><a href="#3-1-2-例子" class="headerlink" title="3.1.2 例子"></a>3.1.2 例子</h5><ul>
<li><p>接下来以一个返回字符串的函数的测试为例，展示测试函数的使用：</p>
<ul>
<li><p>被测试函数：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">HelloTom</span><span class="params">()</span></span> <span class="type">string</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Jerry&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个函数的本意是返回 “Tom” 字符串，但是却返回了 “Jerry”。</p>
</li>
<li><p>测试函数：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">TestHelloTom</span><span class="params">(t *testing.T)</span></span> &#123;</span><br><span class="line">    output := HelloTom()</span><br><span class="line">    expectOutput := <span class="string">&quot;Tom&quot;</span></span><br><span class="line">    <span class="keyword">if</span> output != expectOutput &#123;</span><br><span class="line">        t.Errorf(<span class="string">&quot;Expected %s do not match actual %s&quot;</span>, expectOutput, output)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在测试函数中检测出了问题。</p>
</li>
</ul>
</li>
</ul>
<h5 id="3-1-3-运行"><a href="#3-1-3-运行" class="headerlink" title="3.1.3 运行"></a>3.1.3 运行</h5><ul>
<li><p>在 VScode 中，可以在 <code>Xxx_test.go</code> 文件中，通过点击测试函数左侧的执行符号，方便地进行测试，如下图是测试结果：</p>
<p><img src="/../images/Go%E8%AF%AD%E8%A8%80%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/%E6%B5%8B%E8%AF%95HelloTom-16845038659654.png"></p>
<p>可以看到，检测出了返回字符串不匹配的问题。</p>
</li>
</ul>
<h5 id="★3-1-4-覆盖率"><a href="#★3-1-4-覆盖率" class="headerlink" title="★3.1.4 覆盖率"></a>★3.1.4 覆盖率</h5><ul>
<li><p>代码覆盖率，是一种通过计算测试过程中 <code>被执行的源代码</code> 占 <code>全部源代码</code> 的比例，进而间接度量软件质量的方法。它在保证测试质量的时候潜在保证实际产品的质量，可以基于此在程序中寻找没有被测试用例测试过的地方，进一步创建新的测试用例来增加覆盖率。</p>
</li>
<li><p>在实际项目中，一般覆盖率在 50%~60% 之间，较高覆盖率能达到 80%+。</p>
<p>可以通过下面的方法尽可能地提高覆盖率：</p>
<ol>
<li>测试分支相互独立，全面覆盖。</li>
<li>测试单元粒度足够小，函数单一职责。</li>
</ol>
</li>
</ul>
<h4 id="3-2-单元测试-依赖"><a href="#3-2-单元测试-依赖" class="headerlink" title="3.2 单元测试-依赖"></a>3.2 单元测试-依赖</h4><ul>
<li>单元测试要求<strong>幂等&amp;稳定</strong>：<ul>
<li><strong>幂等</strong>：重复运行测试一个 case 时，其结果应保持相同。</li>
<li><strong>稳定</strong>：单元测试之间是相互隔离的。</li>
</ul>
</li>
</ul>
<h4 id="3-3-单元测试-Mock"><a href="#3-3-单元测试-Mock" class="headerlink" title="3.3 单元测试-Mock"></a>3.3 单元测试-Mock</h4><ul>
<li><p>Mock 意为 “<strong>模仿的；模拟的</strong>”，是指在测试过程中，对于某些不容易构造或者不容易获取的对象，用一个虚拟的 Mock 对象来创建以便测试的测试方法。</p>
<p>例如：在单元测试过程中，数据可能需要经过多方获取，且流程复杂：</p>
<p><img src="/../images/Go%E8%AF%AD%E8%A8%80%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/mock-16845038659666.png"></p>
<p><code>Class A </code>要从 <code>Class B</code> 和<code>Class C</code>中获取数据，而 <code>Class C </code>中该数据又来源于 <code>Class D </code>和 <code>Class E</code>，这时可以利用 Mock 去构造虚拟的<code>Class B</code> 和<code>Class C</code>对象用于<code>Class A </code>的测试，因为我们只是想测试class A的行为是否符合预期，并不需要去测试依赖对象。</p>
</li>
<li><p>常用的 Mock 测试包：<a href="https://github.com/bouk/monkey">bouk&#x2F;monkey: Monkey patching in Go (github.com)</a> </p>
</li>
<li><p>快速 Mock 函数：</p>
<ul>
<li>为一个函数打桩：用打桩函数替代要测试的目标函数，既能对函数的功能进行测试，又不需要对目标函数中所需的文件有强依赖。</li>
<li>为一个方法打桩</li>
</ul>
</li>
</ul>
<h4 id="3-4-基准测试"><a href="#3-4-基准测试" class="headerlink" title="3.4 基准测试"></a>3.4 基准测试</h4><ul>
<li>用于优化代码，对当前代码进行分析。-&gt; 内置的测试框架提供了基准测试的能力。</li>
</ul>
<h3 id="04-项目实践"><a href="#04-项目实践" class="headerlink" title="04 项目实践"></a>04 项目实践</h3><ul>
<li><p>以<strong>掘金社区话题页面</strong>为例，讲述项目开发的流程：</p>
<p><img src="/../images/Go%E8%AF%AD%E8%A8%80%E5%B7%A5%E7%A8%8B%E5%AE%9E%E8%B7%B5/%E7%A4%BE%E5%8C%BA%E8%AF%9D%E9%A2%98%E9%A1%B5%E9%9D%A2-16845038659677.png"></p>
</li>
</ul>
<h4 id="4-1-需求描述"><a href="#4-1-需求描述" class="headerlink" title="4.1 需求描述"></a>4.1 需求描述</h4><ol>
<li>展示话题（标题，文字描述）和回帖列表；</li>
<li>暂不考虑前端页面实现，仅仅实现一个本地 web 服务；</li>
<li>话题和回帖数据用文件存储。</li>
</ol>
<h4 id="4-2-需求用例"><a href="#4-2-需求用例" class="headerlink" title="4.2 需求用例"></a>4.2 需求用例</h4><ul>
<li><p>用 <code>UML</code> 来形象直观地表示需求。在这个项目中，<code>UML</code> 图展示如下：</p>
<img src="../images/Go语言工程实践/UML-16845038659689.png" style="zoom: 50%;" /></li>
</ul>
<h4 id="4-3-ER图"><a href="#4-3-ER图" class="headerlink" title="4.3 ER图"></a>4.3 ER图</h4><ul>
<li><p>ER：Entity Relationship，实体关系。</p>
<p>ER 图表征了现实世界的概念模型及它们之间的关系。在这个项目中，ER 图展示如下：</p>
<img src="../images/Go语言工程实践/ER图-168450386596910.png" style="zoom: 50%;" />

<p>Topic 和 Post 是一对多的关系。</p>
</li>
</ul>
<h4 id="4-4-分层结构"><a href="#4-4-分层结构" class="headerlink" title="4.4 分层结构"></a>4.4 分层结构</h4><ul>
<li><p>主要采用分层结构来进行项目代码的设计，这样做逻辑清晰，职责明确，通过各层之间暴露的接口来进行合作，而不需要知道其他层的代码实现细节，有利于提高代码编写的效率。</p>
</li>
<li><p>分层结构主要分为以下几层：</p>
<img src="../images/Go语言工程实践/分层结构-168450386596911.png" style="zoom:50%;" />

<ul>
<li><strong>数据层</strong>：数据 Model，外部数据的增删改查；</li>
<li><strong>逻辑层</strong>：业务 Entity，处理核心业务逻辑输出；</li>
<li><strong>视图层</strong>：视图 view，处理和外部的交互逻辑。</li>
</ul>
</li>
</ul>
<h4 id="4-5-组件工具"><a href="#4-5-组件工具" class="headerlink" title="4.5 组件工具"></a>4.5 组件工具</h4><ul>
<li><p>我们在搭建社区话题页面时，并不需要从零开始一步步搭建，而可以站在巨人的肩膀上，在他人开源的框架的基础之上进行业务开发。</p>
<p>这是一个开源的 Gin 高性能 go web 框架：<a href="https://github.com/gin-gonic/gin#installation">gin-gonic&#x2F;gin: Gin is a HTTP web framework written in Go (Golang). </a></p>
</li>
</ul>
<h4 id="4-6-项目搭建流程"><a href="#4-6-项目搭建流程" class="headerlink" title="4.6 项目搭建流程"></a>4.6 项目搭建流程</h4><ol>
<li><p>在项目构建之初，我们用 <code>go mod init</code> 进行初始化，然后通过 <code>go get gopkg.in/gin-gonic/gin.v1@v1.3.0</code> 来远程拉取代码包及其依赖包，并自动完成编译和安装。</p>
</li>
<li><p>根据之前的需求分析对<strong>数据层 Repository</strong> 的数据库部分进行代码编写，应该实现两个基本的查询操作：</p>
<ul>
<li>根据话题 Id 查询话题：<code>QueryTopicById</code> </li>
<li>根据话题 Id 查询所有帖子数据：<code>QueryPostByParentId</code></li>
</ul>
</li>
<li><p>在数据库中查询到数据后，将其传输到<strong>逻辑层 Service</strong> 进行处理：参数校验 <code>checkParam</code>  -&gt; 准备数据  <code>prepareInfo</code> -&gt; 组装实体 <code>packPageInfo</code> ；</p>
</li>
<li><p>然后在<strong>视图层 Controller</strong> 将从逻辑层得到的数据显示为话题页面。</p>
</li>
<li><p>最后，利用 <code>gin</code> 这个框架来完成整个项目的实现。包括：</p>
<ol>
<li>初始化数据索引；</li>
<li>初始化引擎配置；</li>
<li>构建路由；</li>
<li>启动服务。</li>
</ol>
</li>
</ol>
<ul>
<li>项目代码可见 <a href="https://github.com/Moonlight-Zhao/go-project-example">Moonlight-Zhao&#x2F;go-project-example (github.com)</a></li>
</ul>
]]></content>
      <categories>
        <category>ByteDance-BackEnd-notebook</category>
      </categories>
  </entry>
  <entry>
    <title>带你认识存储&amp;数据库</title>
    <url>/2023/05/19/%E5%B8%A6%E4%BD%A0%E8%AE%A4%E8%AF%86%E5%AD%98%E5%82%A8-%E6%95%B0%E6%8D%AE%E5%BA%93/</url>
    <content><![CDATA[<h1 id="带你认识存储-amp-数据库"><a href="#带你认识存储-amp-数据库" class="headerlink" title="带你认识存储&amp;数据库"></a>带你认识存储&amp;数据库</h1><h2 id="认识存储与数据库"><a href="#认识存储与数据库" class="headerlink" title="认识存储与数据库"></a>认识存储与数据库</h2><h3 id="01-经典案例"><a href="#01-经典案例" class="headerlink" title="01 经典案例"></a>01 经典案例</h3><ul>
<li>一条数据从产生，到数据流动，最后持久化的全生命周期。</li>
</ul>
<h4 id="1-1-数据的流动"><a href="#1-1-数据的流动" class="headerlink" title="1.1 数据的流动"></a>1.1 数据的流动</h4><ul>
<li><p>以用户注册为例：用户在应用上进行注册后，一条用户注册数据就会被传输到应用的后端服务器。但是如果仅仅将用户注册的信息保存在服务器的内存中，不仅会占用服务器的内存，还有可能造成数据丢失。因此，服务器还要将用户的数据传输到数据库进行保存，以实现数据存储的持久化。</p>
<img src="../images/带你认识存储-数据库/数据流动链路.png" style="zoom: 50%;" /></li>
</ul>
<h4 id="1-2-数据的持久化"><a href="#1-2-数据的持久化" class="headerlink" title="1.2 数据的持久化"></a>1.2 数据的持久化</h4><ul>
<li>数据库在进行数据的持久化时，会进行以下一些操作：<ul>
<li>校验数据的合法性：判断数据是否合法，例如数据是否重复</li>
<li>修改内存：用高效的数据结构组织数据</li>
<li>写入存储介质：以寿命 &amp; 性能友好的方式写入硬件，在对数据进行存储时，还应该考虑硬件的友好性，毕竟最后数据是存储在硬件中的</li>
</ul>
</li>
</ul>
<h3 id="02-存储-amp-数据库简介"><a href="#02-存储-amp-数据库简介" class="headerlink" title="02 存储&amp;数据库简介"></a>02 存储&amp;数据库简介</h3><h4 id="2-1-存储系统"><a href="#2-1-存储系统" class="headerlink" title="2.1 存储系统"></a>2.1 存储系统</h4><h5 id="2-1-1-系统概览"><a href="#2-1-1-系统概览" class="headerlink" title="2.1.1 系统概览"></a>2.1.1 系统概览</h5><ul>
<li><p>什么是存储系统？</p>
<p>​	-&gt; 一个<strong>提供</strong>了读写、控制类<strong>接口</strong>，能够安全有效地<strong>把数据持久化</strong>的软件，就可以称为存储系统。</p>
</li>
</ul>
<h5 id="2-1-2-系统特点"><a href="#2-1-2-系统特点" class="headerlink" title="2.1.2 系统特点"></a>2.1.2 系统特点</h5><ul>
<li>作为后端软件的底座，性能敏感。</li>
<li>存储系统代码，既“简单”又“复杂”：<ul>
<li>“简单” 在于存储系统要求高性能，所以存储系统在 I&#x2F;O 路径上的代码不能写的很复杂，不能有很多分支。</li>
<li>“复杂” 在于在非 I&#x2F;O 路径上、在错误处理路径上，要考虑周全出错的情况，所以代码会写得比较复杂。</li>
</ul>
</li>
<li>存储系统软件架构，容易受硬件影响。</li>
</ul>
<h5 id="2-1-3-存储器层级结构"><a href="#2-1-3-存储器层级结构" class="headerlink" title="2.1.3 存储器层级结构"></a>2.1.3 存储器层级结构</h5><ul>
<li><p>存储器的层级结构即为《计算机组成原理》课程中有关存储器的层级结构，如下图所示：</p>
<p><img src="/../images/%E5%B8%A6%E4%BD%A0%E8%AE%A4%E8%AF%86%E5%AD%98%E5%82%A8-%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%AD%98%E5%82%A8%E5%99%A8%E7%9A%84%E5%B1%82%E7%BA%A7%E7%BB%93%E6%9E%84.png"></p>
</li>
</ul>
<h5 id="2-1-4-RAID技术"><a href="#2-1-4-RAID技术" class="headerlink" title="2.1.4 RAID技术"></a>2.1.4 RAID技术</h5><ul>
<li>RAID：<code>Redundant Array of Inexpensive Disks</code>，磁盘阵列。<ul>
<li>RAID 0：多块磁盘简单组合；数据条带化存储，提高磁盘带宽；没有额外的容错设计。</li>
<li>RAID 1：目的在于提高容错，一块磁盘对应一块额外镜像盘，真实空间利用率仅为 50%（剩下 50% 用于备份），容错能力强。</li>
<li>RAID 0+1：结合了 RAID 0 和RAID 1，真实空间利用率仅 50%，容错能力强，写入带宽好。</li>
</ul>
</li>
</ul>
<h4 id="2-2-数据库"><a href="#2-2-数据库" class="headerlink" title="2.2 数据库"></a>2.2 数据库</h4><ul>
<li>虽然存储系统和数据库的最终目的都是用于存储数据，但是它们不是同一个东西。</li>
<li>数据库分为<strong>关系型数据库</strong>和<strong>非关系型数据库</strong>。</li>
</ul>
<h5 id="2-2-1-概览"><a href="#2-2-1-概览" class="headerlink" title="2.2.1 概览"></a>2.2.1 概览</h5><ul>
<li><p>关系 &#x3D; 集合 &#x3D; 任意元素组成的若干有序偶对，反映了事物间的关系。</p>
<p>关系代数 &#x3D; 对关系作运算的抽象查询语言：交、并、笛卡尔积。</p>
</li>
</ul>
<h5 id="2-2-2-关系型数据库特点"><a href="#2-2-2-关系型数据库特点" class="headerlink" title="2.2.2 关系型数据库特点"></a>2.2.2 关系型数据库特点</h5><ul>
<li>关系型数据库是存储系统，但是在存储之外，又发展出其他能力：<ul>
<li><strong>结构化数据友好</strong></li>
<li><strong>支持事务</strong> ACID</li>
<li><strong>支持复杂查询语言</strong>，如 <code>SQL</code></li>
</ul>
</li>
</ul>
<h5 id="2-2-3-非关系型数据库特点"><a href="#2-2-3-非关系型数据库特点" class="headerlink" title="2.2.3 非关系型数据库特点"></a>2.2.3 非关系型数据库特点</h5><ul>
<li>非关系型数据库也是存储系统，但是<strong>一般不要求严格的结构化</strong>：<ul>
<li><strong>半结构化数据友好</strong></li>
<li>可能支持事务 ACID</li>
<li>可能支持复杂查询语言，如 <code>SQL</code></li>
</ul>
</li>
</ul>
<h4 id="2-3-数据库-vs-经典存储"><a href="#2-3-数据库-vs-经典存储" class="headerlink" title="2.3 数据库 vs 经典存储"></a>2.3 数据库 vs 经典存储</h4><h5 id="2-3-1-结构化数据管理"><a href="#2-3-1-结构化数据管理" class="headerlink" title="2.3.1 结构化数据管理"></a>2.3.1 结构化数据管理</h5><ul>
<li><p>以用户的注册数据为例：</p>
<ul>
<li>对关系型数据库来说，用户的注册数据会以<strong>表形式</strong>管理。</li>
<li>对经典存储来说，我们就要写入文件，<strong>自行定义管理结构</strong>，较为复杂。</li>
</ul>
<img src="../images/带你认识存储-数据库/数据库 vs 经典存储 - 结构化数据管理.png" style="zoom:50%;" /></li>
</ul>
<h5 id="2-3-2-事务能力"><a href="#2-3-2-事务能力" class="headerlink" title="2.3.2 事务能力"></a>2.3.2 事务能力</h5><ul>
<li>关系型数据库支持事务 ACID：<ul>
<li><strong>A</strong>tomicity，事务内的操作要么全做，要么不做；</li>
<li><strong>C</strong>onsistency，事务执行前后，数据状态是一致的；</li>
<li><strong>l</strong>solation，可以隔离多个并发事务，避免影响；</li>
<li><strong>D</strong>urability，事务一旦提交成功，数据保证持久性。</li>
</ul>
</li>
</ul>
<h5 id="2-3-3-复杂查询能力"><a href="#2-3-3-复杂查询能力" class="headerlink" title="2.3.3 复杂查询能力"></a>2.3.3 复杂查询能力</h5><ul>
<li><p>相比于经典存储，关系型数据库可以用 <code>SQL</code> 方便快捷地进行复杂查询，而经典存储可能还要进行一系列的分支、循环等操作才能完成查询要求。</p>
<p>例如：请查询出名字以 xiao 开头，且密码提示问题小于10个字的人，并按性别分组统计人数。</p>
<ul>
<li><p>用 SQL 语句进行查询时，用 <code>Select gender, count(*) from user where user_name like &#39;xiao%&#39; and len(password_hint) &lt; 10 group by gender;</code> 即可；</p>
</li>
<li><p>而用经典存储时，则需要复杂的代码操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for each data &#123; </span><br><span class="line">    if (user_name... &amp;&amp; password_hint...) &#123;</span><br><span class="line">        mark in list </span><br><span class="line">    &#125; </span><br><span class="line">&#125; </span><br><span class="line">for each in marked_list &#123;</span><br><span class="line">    if (gender == ...)&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125; </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure></li>
</ul>
</li>
</ul>
<h2 id="主流存储产品剖析"><a href="#主流存储产品剖析" class="headerlink" title="主流存储产品剖析"></a>主流存储产品剖析</h2><h3 id="03-主流产品剖析"><a href="#03-主流产品剖析" class="headerlink" title="03 主流产品剖析"></a>03 主流产品剖析</h3><h4 id="3-1-单机存储"><a href="#3-1-单机存储" class="headerlink" title="3.1 单机存储"></a>3.1 单机存储</h4><ul>
<li>单机存储 &#x3D; 单个计算机节点上的存储软件系统，一般不涉及网络交互。</li>
</ul>
<h5 id="3-1-1-本地文件系统"><a href="#3-1-1-本地文件系统" class="headerlink" title="3.1.1 本地文件系统"></a>3.1.1 本地文件系统</h5><ul>
<li><p>文件系统的管理单元 : 文件。</p>
</li>
<li><p>文件系统接口 : 文件系统繁多，如 <code>Ext2/3/4</code>、<code>sysfs</code>、 <code>rootfs</code> 等，但都遵循 <code>VFS</code> 的统一抽象接口。</p>
<ul>
<li>背后逻辑在于：虽然有各种各样的硬件，但是为了上层应用使用的方便，这些硬件都应该有统一的接口规则，这样上层应用就不需要关心不同硬件的使用细节，而只需要会使用统一的接口即可。</li>
</ul>
</li>
<li><p><code>Linux</code> 文件系统的两大数据结构 : <code>Index Node</code> &amp; <code>Directory Entry</code> </p>
<p>![](..&#x2F;images&#x2F;带你认识存储-数据库&#x2F;Index Node.png)</p>
<p>![](..&#x2F;images&#x2F;带你认识存储-数据库&#x2F;Directory Entry.png)</p>
</li>
</ul>
<h5 id="3-1-2-key-value存储"><a href="#3-1-2-key-value存储" class="headerlink" title="3.1.2 key-value存储"></a>3.1.2 key-value存储</h5><ul>
<li>所谓的 <code>Key-Value</code> 就是每次存储一个数据时，是根据 <code>Key</code> 进行索引存储的，<code>Value</code> 是对应该 <code>Key</code> 的值。</li>
<li>常见使用方式：<code>put(k, v)</code> 、<code>get(k)</code> </li>
<li>常见数据结构：<code>LSM-Tree</code>，某种程度上牺牲读性能，追求写入性能。</li>
</ul>
<h4 id="3-2-分布式存储"><a href="#3-2-分布式存储" class="headerlink" title="3.2 分布式存储"></a>3.2 分布式存储</h4><ul>
<li>分布式存储 &#x3D; 在单机存储基础上实现了分布式协议，涉及大量网络交互。</li>
</ul>
<h5 id="3-2-1-分布式文件系统HDFS"><a href="#3-2-1-分布式文件系统HDFS" class="headerlink" title="3.2.1 分布式文件系统HDFS"></a>3.2.1 分布式文件系统HDFS</h5><ul>
<li><code>HDFS</code>核心特点：<ul>
<li>支持<strong>海量数据存储</strong> </li>
<li><strong>高容错性</strong> </li>
<li>弱 POSIX 语义</li>
<li>使用普通 x86 服务器，<strong>性价比高</strong></li>
</ul>
</li>
</ul>
<h5 id="3-2-2-Ceph"><a href="#3-2-2-Ceph" class="headerlink" title="3.2.2 Ceph"></a>3.2.2 Ceph</h5><ul>
<li><code>Ceph</code> 核心特点：<ul>
<li>一套系统支持对象接口、块接口、文件接口，但是<strong>一切皆对象</strong></li>
<li>数据写入采用<strong>主备复制模型</strong></li>
<li>数据分布模型采用 <strong>CRUSH</strong> 算法</li>
</ul>
</li>
</ul>
<h4 id="3-3-单机关系型数据库"><a href="#3-3-单机关系型数据库" class="headerlink" title="3.3 单机关系型数据库"></a>3.3 单机关系型数据库</h4><ul>
<li>单机数据库 &#x3D; 单个计算机节点上的数据库系统</li>
<li>事务在单机内执行，也可能通过网络交互实现分布式事务</li>
<li>在关系型数据库中，常见产品有 <code>Oracle</code>、<code>MySQL</code>、<code>PostgreSQL</code>，其中 <code>Oracle</code> 是商用的，后两者是开源的。</li>
</ul>
<h5 id="3-3-1-关系型数据库的通用组件"><a href="#3-3-1-关系型数据库的通用组件" class="headerlink" title="3.3.1 关系型数据库的通用组件"></a>3.3.1 关系型数据库的通用组件</h5><ul>
<li>Query Engine：负责解析 query，生成查询计划</li>
<li>Txn Manager：负责事务并发管理</li>
<li>Lock Manager：负责锁相关的策略</li>
<li>Storage Engine：负责组织内存&#x2F;磁盘数据结构</li>
<li>Replication：负责主备同步</li>
</ul>
<h5 id="3-3-2-关系型数据库结构解析"><a href="#3-3-2-关系型数据库结构解析" class="headerlink" title="3.3.2 关系型数据库结构解析"></a>3.3.2 关系型数据库结构解析</h5><ul>
<li><p>如图所示，在关系型数据库中，内存部分的数据是以树的形式来组织的，每一个节点都代表一页Page；除了树之外，还有大量的 <code>Temp data</code>，用于存储临时的数据。</p>
<p>在硬盘中，有三类文件：</p>
<ul>
<li><code>Page Files</code>：对应于内存树中的Page；</li>
<li><code>Redo Log Files</code>：记录了事务执行过程中的 <code>Redo Log</code>；</li>
<li><code>Others</code>：可以用来存储临时数据文件。</li>
</ul>
<p><img src="/../images/%E5%B8%A6%E4%BD%A0%E8%AE%A4%E8%AF%86%E5%AD%98%E5%82%A8-%E6%95%B0%E6%8D%AE%E5%BA%93/%E5%85%B3%E7%B3%BB%E5%9E%8B%E6%95%B0%E6%8D%AE%E5%BA%93.png"></p>
</li>
</ul>
<h4 id="3-4-单机非关系型数据库"><a href="#3-4-单机非关系型数据库" class="headerlink" title="3.4 单机非关系型数据库"></a>3.4 单机非关系型数据库</h4><ul>
<li><p>在非关系型数据库中，常见产品有 <code>MongoDB</code>、<code>Redis</code>、<code>Elasticsearch</code>。</p>
</li>
<li><p>关系型数据库一般直接使用 SQL 交互，而<strong>非关系型数据库交互方式各不相同</strong>。</p>
</li>
<li><p>非关系型数据库的数据结构千奇百怪，没有关系约束后，schema相对灵活。</p>
</li>
<li><p>不管是否关系型数据库，大家都在尝试支持 SQL(子集)和 “事务”。</p>
</li>
</ul>
<h5 id="3-4-1-Elasticsearch"><a href="#3-4-1-Elasticsearch" class="headerlink" title="3.4.1 Elasticsearch"></a>3.4.1 Elasticsearch</h5><ul>
<li>面向「文档」存储</li>
<li>文档可序列化成 <code>JSON</code>，支持嵌套</li>
<li>存在「index」，index &#x3D; 文档的集合</li>
<li>存储和构建索引能力依赖 Lucene 引攀</li>
<li>实现了大量搜索数据结构 &amp; 算法</li>
<li>支持 <code>RESTFUL API</code>，也支持弱 <code>SQL</code> 交互</li>
</ul>
<h5 id="3-4-2-MongoDB"><a href="#3-4-2-MongoDB" class="headerlink" title="3.4.2 MongoDB"></a>3.4.2 MongoDB</h5><ul>
<li>面向「文档」存储</li>
<li>文档可序列化成 <code>JSON/BSON</code>，支持嵌套</li>
<li>存在「collection」，collection &#x3D; 文档的集合</li>
<li>存储和构建索引能力依赖 wiredTiger 引擎</li>
<li>4.0 后开始支持事务（多文档、跨分片多文档等）</li>
<li>常用 <code>client/SDK</code> 交互，可通过插件转译支持弱 <code>SQL</code></li>
</ul>
<h5 id="3-4-3-Redis"><a href="#3-4-3-Redis" class="headerlink" title="3.4.3 Redis"></a>3.4.3 Redis</h5><ul>
<li>数据结构丰富（hash表、set、Zset、Iist）</li>
<li>C语言实现，超高性能</li>
<li>主要基于内存，但支持 AOF&#x2F;RDB 特久化</li>
<li>常用 <code>redis-di</code>&#x2F;多语言 SDK 交互</li>
</ul>
<h4 id="3-5-分布式数据库"><a href="#3-5-分布式数据库" class="headerlink" title="3.5 分布式数据库"></a>3.5 分布式数据库</h4><ul>
<li><p>鉴于单机数据库遇到的如下问题：</p>
<ul>
<li>容量：单点容量有限，受硬件限制；</li>
<li>弹性：用户的需求在弹性变化；</li>
<li>性价比：单机的硬件参数是固定的，可能会有资源浪费；</li>
</ul>
<p>我们开始采用分布式数据库（<strong>池化技术</strong>）。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>ByteDance-BackEnd-notebook</category>
      </categories>
  </entry>
  <entry>
    <title>Go语言基础语法</title>
    <url>/2023/05/19/Go%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<h1 id="Go语言基础语法"><a href="#Go语言基础语法" class="headerlink" title="Go语言基础语法"></a>Go语言基础语法</h1><h2 id="Go-语言快速上手-基础语言"><a href="#Go-语言快速上手-基础语言" class="headerlink" title="Go 语言快速上手 - 基础语言"></a>Go 语言快速上手 - 基础语言</h2><h3 id="01-简介"><a href="#01-简介" class="headerlink" title="01 简介"></a>01 简介</h3><h4 id="1-1-什么是-Go-语言"><a href="#1-1-什么是-Go-语言" class="headerlink" title="1.1 什么是 Go 语言"></a>1.1 什么是 Go 语言</h4><ul>
<li>Go 语言是谷歌出品的一门通用型计算机编程语言。有以下特性：<ol>
<li>高性能、高并发：内嵌了对高并发的支持</li>
<li>语法简单、学习曲线平缓</li>
<li>丰富的标准库</li>
<li>完善的工具链：编译、代码格式化、错误检查、代码文档、包管理、代码提示</li>
<li>静态链接</li>
<li>快速编译</li>
<li>跨平台：Linux、WIndows、MacOS、Android、ios</li>
<li>垃圾回收</li>
</ol>
</li>
</ul>
<h4 id="1-2-哪些公司在使用-GO"><a href="#1-2-哪些公司在使用-GO" class="headerlink" title="1.2 哪些公司在使用 GO"></a>1.2 哪些公司在使用 GO</h4><ul>
<li>字节、腾讯、哔哩哔哩、Google、Facebook等</li>
</ul>
<h4 id="1-3-字节全面拥抱-Go-的原因"><a href="#1-3-字节全面拥抱-Go-的原因" class="headerlink" title="1.3 字节全面拥抱 Go 的原因"></a>1.3 字节全面拥抱 Go 的原因</h4><ol>
<li>最初使用的 Python，由于性能问题换成了 Go</li>
<li>C++ 不太适合在线 Web 业务</li>
<li>早期团队非 Java 背景</li>
<li>性能比较好</li>
<li>部署简单、学习成本低</li>
<li>内部 RPC 和 HTTP 框架的推广</li>
</ol>
<h3 id="02-入门"><a href="#02-入门" class="headerlink" title="02 入门"></a>02 入门</h3><h4 id="2-1-开发环境"><a href="#2-1-开发环境" class="headerlink" title="2.1 开发环境"></a>2.1 开发环境</h4><h5 id="安装-Golang"><a href="#安装-Golang" class="headerlink" title="安装 Golang"></a>安装 Golang</h5><ul>
<li><p>通过以下链接可以下载安装 Golang：</p>
<p><a href="https://go.dev/">The Go Programming Language</a> </p>
<p><a href="https://studygolang.com/dl">Go下载 - Go语言中文网</a> </p>
<p><a href="https://goproxy.cn/">七牛云 - Goproxy.cn</a></p>
</li>
</ul>
<h5 id="配置集成开发环境"><a href="#配置集成开发环境" class="headerlink" title="配置集成开发环境"></a>配置集成开发环境</h5><ul>
<li>可以使用 VScode 作为 Go 的集成开发环境</li>
</ul>
<h4 id="2-2-基础语法"><a href="#2-2-基础语法" class="headerlink" title="2.2 基础语法"></a>2.2 基础语法</h4><ul>
<li><p>以 “hello world” 为例，介绍 go 的代码组成：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	fmt.Println(<span class="string">&quot;hello world&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>package main</code> ：代表这个文件属于 main 包的一部分，是程序的入口文件</li>
<li><code>import &#123;&quot;fmt&quot;&#125;</code>：导入了 “fmt” 包，用于向屏幕输入输出字符串、格式化字符串</li>
<li><code>func main() &#123;fmt.Println(&quot;hello world&quot;)&#125;</code>：main 函数，用于向屏幕输出 “hello world” 字符串</li>
</ul>
</li>
</ul>
<h5 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h5><ul>
<li>Go 语言是强类型语言。</li>
<li>Go 语言变量的声明有两种方式：<ol>
<li><code>var name = value</code> ：这种方式会自动推导变量的类型</li>
<li><code>name := value</code></li>
</ol>
</li>
</ul>
<h5 id="常量"><a href="#常量" class="headerlink" title="常量"></a>常量</h5><ul>
<li>Go 语言常量的声明方式：<code>const name = value</code>：会根据使用的上下文自动确定使用类型</li>
</ul>
<h5 id="if-else"><a href="#if-else" class="headerlink" title="if-else"></a>if-else</h5><ul>
<li><p><code>if</code> 后面的条件判断不带括号，且<strong>相应的结果语句必须带大括号</strong>，不能和 <code>if</code> 写在同一行。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> <span class="number">7</span>%<span class="number">2</span> == <span class="number">0</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;7 is even&quot;</span>)</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;7 is odd&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> <span class="number">8</span>%<span class="number">4</span> == <span class="number">0</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;8 is divisible by 4&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> num := <span class="number">9</span>; num &lt; <span class="number">0</span> &#123;</span><br><span class="line">		fmt.Println(num, <span class="string">&quot;is negative&quot;</span>)</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> num &lt; <span class="number">10</span> &#123;</span><br><span class="line">		fmt.Println(num, <span class="string">&quot;has 1 digit&quot;</span>)</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		fmt.Println(num, <span class="string">&quot;has multiple digits&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="循环"><a href="#循环" class="headerlink" title="循环"></a>循环</h5><ul>
<li><p><strong>Go 只有 <code>for</code> 循环</strong>。</p>
<p><code>for</code> 循环后面也不用带括号，可以用 <code>break</code>、<code>continue</code> 来对循环进行操作。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	i := <span class="number">1</span></span><br><span class="line">	<span class="keyword">for</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;loop&quot;</span>)</span><br><span class="line">		<span class="keyword">break</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> j := <span class="number">7</span>; j &lt; <span class="number">9</span>; j++ &#123;</span><br><span class="line">		fmt.Println(j)</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> n := <span class="number">0</span>; n &lt; <span class="number">5</span>; n++ &#123;</span><br><span class="line">		<span class="keyword">if</span> n%<span class="number">2</span> == <span class="number">0</span> &#123;</span><br><span class="line">			<span class="keyword">continue</span></span><br><span class="line">		&#125;</span><br><span class="line">		fmt.Println(n)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> i &lt;= <span class="number">3</span> &#123;</span><br><span class="line">		fmt.Println(i)</span><br><span class="line">		i = i + <span class="number">1</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="switch"><a href="#switch" class="headerlink" title="switch"></a>switch</h5><ul>
<li><p><code>switch</code> 后面的变量名也不需要括号。在 <code>switch</code> 的 case 中，如果没有 <code>break</code>，是不会像 <code>C++</code> 一样一直向下执行其他的 case 的。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	a := <span class="number">2</span></span><br><span class="line">	<span class="keyword">switch</span> a &#123;</span><br><span class="line">	<span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;one&quot;</span>)</span><br><span class="line">	<span class="keyword">case</span> <span class="number">2</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;two&quot;</span>)</span><br><span class="line">	<span class="keyword">case</span> <span class="number">3</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;three&quot;</span>)</span><br><span class="line">	<span class="keyword">case</span> <span class="number">4</span>, <span class="number">5</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;four or five&quot;</span>)</span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;other&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	t := time.Now()</span><br><span class="line">	<span class="keyword">switch</span> &#123;</span><br><span class="line">	<span class="keyword">case</span> t.Hour() &lt; <span class="number">12</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;It&#x27;s before noon&quot;</span>)</span><br><span class="line">		fmt.Println(t.Hour())</span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">		fmt.Println(<span class="string">&quot;It&#x27;s after noon&quot;</span>)</span><br><span class="line">		fmt.Println(t.Hour())</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h5><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">var</span> a [<span class="number">5</span>]<span class="type">int</span></span><br><span class="line">	a[<span class="number">4</span>] = <span class="number">100</span></span><br><span class="line">	fmt.Println(<span class="string">&quot;get:&quot;</span>, a[<span class="number">2</span>])</span><br><span class="line">	fmt.Println(<span class="string">&quot;len:&quot;</span>, <span class="built_in">len</span>(a))</span><br><span class="line"></span><br><span class="line">	b := [<span class="number">5</span>]<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;</span><br><span class="line">	fmt.Println(b)</span><br><span class="line"></span><br><span class="line">	<span class="keyword">var</span> twoD [<span class="number">2</span>][<span class="number">3</span>]<span class="type">int</span></span><br><span class="line">	<span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">2</span>; i++ &#123;</span><br><span class="line">		<span class="keyword">for</span> j := <span class="number">0</span>; j &lt; <span class="number">3</span>; j++ &#123;</span><br><span class="line">			twoD[i][j] = i + j</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(<span class="string">&quot;2d: &quot;</span>, twoD)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="★切片"><a href="#★切片" class="headerlink" title="★切片"></a>★切片</h5><ul>
<li><p>切片不同于数组，它是一个可变长度的数组，可以任意更改大小。</p>
<p>我们用 <code>make</code> 来创建切片。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	s := <span class="built_in">make</span>([]<span class="type">string</span>, <span class="number">3</span>)</span><br><span class="line">	s[<span class="number">0</span>] = <span class="string">&quot;a&quot;</span></span><br><span class="line">	s[<span class="number">1</span>] = <span class="string">&quot;b&quot;</span></span><br><span class="line">	s[<span class="number">2</span>] = <span class="string">&quot;c&quot;</span></span><br><span class="line">	fmt.Println(<span class="string">&quot;get:&quot;</span>, s[<span class="number">2</span>])   <span class="comment">// c</span></span><br><span class="line">	fmt.Println(<span class="string">&quot;len:&quot;</span>, <span class="built_in">len</span>(s)) <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line">	s = <span class="built_in">append</span>(s, <span class="string">&quot;d&quot;</span>)</span><br><span class="line">	s = <span class="built_in">append</span>(s, <span class="string">&quot;e&quot;</span>, <span class="string">&quot;f&quot;</span>)</span><br><span class="line">	fmt.Println(s) <span class="comment">// [a b c d e f]</span></span><br><span class="line"></span><br><span class="line">	c := <span class="built_in">make</span>([]<span class="type">string</span>, <span class="built_in">len</span>(s))</span><br><span class="line">	<span class="built_in">copy</span>(c, s)</span><br><span class="line">	fmt.Println(c) <span class="comment">// [a b c d e f]</span></span><br><span class="line"></span><br><span class="line">	fmt.Println(s[<span class="number">2</span>:<span class="number">5</span>]) <span class="comment">// [c d e]</span></span><br><span class="line">	fmt.Println(s[:<span class="number">5</span>])  <span class="comment">// [a b c d e]</span></span><br><span class="line">	fmt.Println(s[<span class="number">2</span>:])  <span class="comment">// [c d e f]</span></span><br><span class="line"></span><br><span class="line">	good := []<span class="type">string</span>&#123;<span class="string">&quot;g&quot;</span>, <span class="string">&quot;o&quot;</span>, <span class="string">&quot;o&quot;</span>, <span class="string">&quot;d&quot;</span>&#125;</span><br><span class="line">	fmt.Println(good) <span class="comment">// [g o o d]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="★map"><a href="#★map" class="headerlink" title="★map"></a>★map</h5><ul>
<li><p>实际使用过程中使用最频繁的数据结构。</p>
<p>我们<strong>用 <code>make(map[key]value)</code> 来创建 map</strong>。</p>
<p>Go 里面的 map 是完全无序的。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	m := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="type">string</span>]<span class="type">int</span>)</span><br><span class="line">	m[<span class="string">&quot;one&quot;</span>] = <span class="number">1</span></span><br><span class="line">	m[<span class="string">&quot;two&quot;</span>] = <span class="number">2</span></span><br><span class="line">	fmt.Println(m)           <span class="comment">// map[one:1 two:2]</span></span><br><span class="line">	fmt.Println(<span class="built_in">len</span>(m))      <span class="comment">// 2</span></span><br><span class="line">	fmt.Println(m[<span class="string">&quot;one&quot;</span>])    <span class="comment">// 1</span></span><br><span class="line">	fmt.Println(m[<span class="string">&quot;unknow&quot;</span>]) <span class="comment">// 0</span></span><br><span class="line"></span><br><span class="line">	r, ok := m[<span class="string">&quot;unknow&quot;</span>]</span><br><span class="line">	fmt.Println(r, ok) <span class="comment">// 0 false</span></span><br><span class="line"></span><br><span class="line">	<span class="built_in">delete</span>(m, <span class="string">&quot;one&quot;</span>)</span><br><span class="line"></span><br><span class="line">	m2 := <span class="keyword">map</span>[<span class="type">string</span>]<span class="type">int</span>&#123;<span class="string">&quot;one&quot;</span>: <span class="number">1</span>, <span class="string">&quot;two&quot;</span>: <span class="number">2</span>&#125;</span><br><span class="line">	<span class="keyword">var</span> m3 = <span class="keyword">map</span>[<span class="type">string</span>]<span class="type">int</span>&#123;<span class="string">&quot;one&quot;</span>: <span class="number">1</span>, <span class="string">&quot;two&quot;</span>: <span class="number">2</span>&#125;</span><br><span class="line">	fmt.Println(m2, m3)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="range"><a href="#range" class="headerlink" title="range"></a>range</h5><ul>
<li><p>对于一个 <code>slice</code> 或者 <code>map</code>，我们可以用 <code>range</code> 来进行<strong>快速遍历</strong>。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	nums := []<span class="type">int</span>&#123;<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>&#125;</span><br><span class="line">	sum := <span class="number">0</span></span><br><span class="line">	<span class="keyword">for</span> i, num := <span class="keyword">range</span> nums &#123;</span><br><span class="line">		sum += num</span><br><span class="line">		<span class="keyword">if</span> num == <span class="number">2</span> &#123;</span><br><span class="line">			fmt.Println(<span class="string">&quot;index:&quot;</span>, i, <span class="string">&quot;num:&quot;</span>, num) <span class="comment">// index: 0 num: 2</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(sum) <span class="comment">// 9</span></span><br><span class="line"></span><br><span class="line">	m := <span class="keyword">map</span>[<span class="type">string</span>]<span class="type">string</span>&#123;<span class="string">&quot;a&quot;</span>: <span class="string">&quot;A&quot;</span>, <span class="string">&quot;b&quot;</span>: <span class="string">&quot;B&quot;</span>&#125;</span><br><span class="line">	<span class="keyword">for</span> k, v := <span class="keyword">range</span> m &#123;</span><br><span class="line">		fmt.Println(k, v) <span class="comment">// b 8; a A</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> k := <span class="keyword">range</span> m &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;key&quot;</span>, k) <span class="comment">// key a; key b</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h5><ul>
<li><p>Go 里面的函数会返回多个值，第一个值是真正的返回结果，第二个值返回的是错误信息。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add</span><span class="params">(a <span class="type">int</span>, b <span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> a + b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add2</span><span class="params">(a, b <span class="type">int</span>)</span></span> <span class="type">int</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> a + b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">exists</span><span class="params">(m <span class="keyword">map</span>[<span class="type">string</span>]<span class="type">string</span>, k <span class="type">string</span>)</span></span> (v <span class="type">string</span>, ok <span class="type">bool</span>) &#123;</span><br><span class="line">	v, ok = m[k]</span><br><span class="line">	<span class="keyword">return</span> v, ok</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	res := add(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">	fmt.Println(res) <span class="comment">// 3</span></span><br><span class="line"></span><br><span class="line">	v, ok := exists(<span class="keyword">map</span>[<span class="type">string</span>]<span class="type">string</span>&#123;<span class="string">&quot;a&quot;</span>: <span class="string">&quot;A&quot;</span>&#125;, <span class="string">&quot;a&quot;</span>)</span><br><span class="line">	fmt.Println(v, ok) <span class="comment">// A True</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="指针"><a href="#指针" class="headerlink" title="指针"></a>指针</h5><ul>
<li><p>主要用于对传入的参数进行修改。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add2</span><span class="params">(n <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">	n += <span class="number">2</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">add2ptr</span><span class="params">(n *<span class="type">int</span>)</span></span> &#123;</span><br><span class="line">	*n += <span class="number">2</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	n := <span class="number">5</span></span><br><span class="line">	add2(n)</span><br><span class="line">	fmt.Println(n) <span class="comment">// 5</span></span><br><span class="line">	add2ptr(&amp;n)</span><br><span class="line">	fmt.Println(n) <span class="comment">// 7</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h5><ul>
<li><p>结构体是带类型的字段的集合。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> user <span class="keyword">struct</span> &#123;</span><br><span class="line">	name     <span class="type">string</span></span><br><span class="line">	password <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	a := user&#123;name: <span class="string">&quot;wang&quot;</span>, password: <span class="string">&quot;1024&quot;</span>&#125;</span><br><span class="line">	b := user&#123;<span class="string">&quot;wang&quot;</span>, <span class="string">&quot;1024&quot;</span>&#125;</span><br><span class="line">	c := user&#123;name: <span class="string">&quot;wang&quot;</span>&#125;</span><br><span class="line">	c.password = <span class="string">&quot;1024&quot;</span></span><br><span class="line">	<span class="keyword">var</span> d user</span><br><span class="line">	d.name = <span class="string">&quot;wang&quot;</span></span><br><span class="line">	d.password = <span class="string">&quot;1024&quot;</span></span><br><span class="line"></span><br><span class="line">	fmt.Println(a, b, c, d)                 <span class="comment">// &#123;wang 1024&#125; &#123;wang 1024&#125; &#123;wang 1024&#125; &#123;wang 1024&#125;</span></span><br><span class="line">	fmt.Println(checkPassword(a, <span class="string">&quot;haha&quot;</span>))   <span class="comment">// false</span></span><br><span class="line">	fmt.Println(checkPassword2(&amp;a, <span class="string">&quot;haha&quot;</span>)) <span class="comment">// false</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">checkPassword</span><span class="params">(u user, password <span class="type">string</span>)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> u.password == password</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">checkPassword2</span><span class="params">(u *user, password <span class="type">string</span>)</span></span> <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> u.password == password</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="结构体方法"><a href="#结构体方法" class="headerlink" title="结构体方法"></a>结构体方法</h5><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> user <span class="keyword">struct</span> &#123;</span><br><span class="line">	name     <span class="type">string</span></span><br><span class="line">	password <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(u user)</span></span> checkPassword(password <span class="type">string</span>) <span class="type">bool</span> &#123;</span><br><span class="line">	<span class="keyword">return</span> u.password == password</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(u *user)</span></span> resetPassword(password <span class="type">string</span>) &#123;</span><br><span class="line">	u.password = password</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	a := user&#123;name: <span class="string">&quot;wang&quot;</span>, password: <span class="string">&quot;1024&quot;</span>&#125;</span><br><span class="line">	a.resetPassword(<span class="string">&quot;2048&quot;</span>)</span><br><span class="line">	fmt.Println(a.checkPassword(<span class="string">&quot;2048&quot;</span>)) <span class="comment">// true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h5><ul>
<li><p>前面说过，Go 里面的函数会返回多个值，第一个值是真正的返回结果，第二个值返回的是错误信息。</p>
<p>我们可以通过函数返回的错误信息，来判断是否发生了某种错误。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;errors&quot;</span></span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> user <span class="keyword">struct</span> &#123;</span><br><span class="line">	name     <span class="type">string</span></span><br><span class="line">	password <span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">findUser</span><span class="params">(users []user, name <span class="type">string</span>)</span></span> (v *user, err <span class="type">error</span>) &#123;</span><br><span class="line">	<span class="keyword">for</span> _, u := <span class="keyword">range</span> users &#123;</span><br><span class="line">		<span class="keyword">if</span> u.name == name &#123;</span><br><span class="line">			<span class="keyword">return</span> &amp;u, <span class="literal">nil</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">nil</span>, errors.New(<span class="string">&quot;not found&quot;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	u, err := findUser([]user&#123;&#123;<span class="string">&quot;wang&quot;</span>, <span class="string">&quot;1024&quot;</span>&#125;&#125;, <span class="string">&quot;wang&quot;</span>)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		fmt.Println(err)</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(u.name) <span class="comment">// wang</span></span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> u, err := findUser([]user&#123;&#123;<span class="string">&quot;wang&quot;</span>, <span class="string">&quot;1024&quot;</span>&#125;&#125;, <span class="string">&quot;li&quot;</span>); err != <span class="literal">nil</span> &#123;</span><br><span class="line">		fmt.Println(err) <span class="comment">// not found</span></span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		fmt.Println(u.name)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在这里，<code>nil</code> 用于表示是否发生了错误，如果返回的 <code>error</code> 为 <code>nil</code>，则表示没有发生错误；否则发生了错误。</p>
<p>在 Go 中，为了简单和方便，<strong>nil 被设计为一个标识符，可以用来表示某些类型的零值</strong>。他不是一个不变的值，而是随着不同类型有不同的类型，在日常的编码中，编译器会根据上下文来推导出。</p>
</li>
</ul>
<h5 id="字符串操作"><a href="#字符串操作" class="headerlink" title="字符串操作"></a>字符串操作</h5><ul>
<li><p>在 Go 的标准库 strings 中，有非常多的字符串操作函数，如下所示：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;strings&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	a := <span class="string">&quot;hello&quot;</span></span><br><span class="line">	fmt.Println(strings.Contains(a, <span class="string">&quot;ll&quot;</span>))                <span class="comment">// true</span></span><br><span class="line">	fmt.Println(strings.Count(a, <span class="string">&quot;l&quot;</span>))                    <span class="comment">// 2</span></span><br><span class="line">	fmt.Println(strings.HasPrefix(a, <span class="string">&quot;he&quot;</span>))               <span class="comment">// true</span></span><br><span class="line">	fmt.Println(strings.HasSuffix(a, <span class="string">&quot;llo&quot;</span>))              <span class="comment">// true</span></span><br><span class="line">	fmt.Println(strings.Index(a, <span class="string">&quot;ll&quot;</span>))                   <span class="comment">// 2</span></span><br><span class="line">	fmt.Println(strings.Join([]<span class="type">string</span>&#123;<span class="string">&quot;he&quot;</span>, <span class="string">&quot;llo&quot;</span>&#125;, <span class="string">&quot;-&quot;</span>)) <span class="comment">// he-llo</span></span><br><span class="line">	fmt.Println(strings.Repeat(a, <span class="number">2</span>))                     <span class="comment">// hellohello</span></span><br><span class="line">	fmt.Println(strings.Replace(a, <span class="string">&quot;e&quot;</span>, <span class="string">&quot;E&quot;</span>, <span class="number">-1</span>))         <span class="comment">// hEllo</span></span><br><span class="line">	fmt.Println(strings.Split(<span class="string">&quot;a-b-c&quot;</span>, <span class="string">&quot;-&quot;</span>))              <span class="comment">// [a b c]</span></span><br><span class="line">	fmt.Println(strings.ToLower(a))                       <span class="comment">// hello</span></span><br><span class="line">	fmt.Println(strings.ToUpper(a))                       <span class="comment">// HELLO</span></span><br><span class="line">	fmt.Println(<span class="built_in">len</span>(a))                                   <span class="comment">// 5</span></span><br><span class="line">	b := <span class="string">&quot;你好&quot;</span></span><br><span class="line">	fmt.Println(<span class="built_in">len</span>(b)) <span class="comment">// 6</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="字符串格式化"><a href="#字符串格式化" class="headerlink" title="字符串格式化"></a>字符串格式化</h5><ul>
<li><p>在 Go 的标准库 fmt 中，可以用 <code>Printf</code> 对输出进行格式化：</p>
<p>通过 <code>%+v</code>、<code>%#v</code> 可以获得更详细的输出。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> point <span class="keyword">struct</span> &#123;</span><br><span class="line">	x, y <span class="type">int</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	s := <span class="string">&quot;hello&quot;</span></span><br><span class="line">	n := <span class="number">123</span></span><br><span class="line">	p := point&#123;<span class="number">1</span>, <span class="number">2</span>&#125;</span><br><span class="line">	fmt.Println(s, n) <span class="comment">// hello 123</span></span><br><span class="line">	fmt.Println(p)    <span class="comment">// &#123;1 2&#125;</span></span><br><span class="line"></span><br><span class="line">	fmt.Printf(<span class="string">&quot;s=%v\n&quot;</span>, s)  <span class="comment">// s=hello</span></span><br><span class="line">	fmt.Printf(<span class="string">&quot;n=%v\n&quot;</span>, n)  <span class="comment">// n=123</span></span><br><span class="line">	fmt.Printf(<span class="string">&quot;p=%v\n&quot;</span>, p)  <span class="comment">// p=&#123;1 2&#125;</span></span><br><span class="line">	fmt.Printf(<span class="string">&quot;p=%+v\n&quot;</span>, p) <span class="comment">// p=&#123;x:1 y:2&#125;</span></span><br><span class="line">	fmt.Printf(<span class="string">&quot;p=%#v\n&quot;</span>, p) <span class="comment">// p=main.point&#123;x:1, y:2&#125;</span></span><br><span class="line"></span><br><span class="line">	f := <span class="number">3.141592653</span></span><br><span class="line">	fmt.Println(f)          <span class="comment">// 3.141592653</span></span><br><span class="line">	fmt.Printf(<span class="string">&quot;%.2f\n&quot;</span>, f) <span class="comment">// 3.14</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="★JSON处理"><a href="#★JSON处理" class="headerlink" title="★JSON处理"></a>★JSON处理</h5><ul>
<li><p>通过 <code>json.Marshal</code> 函数进行 JSON 处理。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;encoding/json&quot;</span></span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> userInfo <span class="keyword">struct</span> &#123;</span><br><span class="line">	Name  <span class="type">string</span></span><br><span class="line">	Age   <span class="type">int</span> <span class="string">`json:&quot;age&quot;`</span></span><br><span class="line">	Hobby []<span class="type">string</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	a := userInfo&#123;Name: <span class="string">&quot;wang&quot;</span>, Age: <span class="number">18</span>, Hobby: []<span class="type">string</span>&#123;<span class="string">&quot;Golang&quot;</span>, <span class="string">&quot;TypeScript&quot;</span>&#125;&#125;</span><br><span class="line">	buf, err := json.Marshal(a)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(buf)         <span class="comment">// [123 34 78 97...]</span></span><br><span class="line">	fmt.Println(<span class="type">string</span>(buf)) <span class="comment">// &#123;&quot;Name&quot;:&quot;wang&quot;,&quot;age&quot;:18,&quot;Hobby&quot;:[&quot;Golang&quot;,&quot;TypeScript&quot;]&#125;</span></span><br><span class="line"></span><br><span class="line">	buf, err = json.MarshalIndent(a, <span class="string">&quot;&quot;</span>, <span class="string">&quot;\t&quot;</span>)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(<span class="type">string</span>(buf))</span><br><span class="line"></span><br><span class="line">	<span class="keyword">var</span> b userInfo</span><br><span class="line">	err = json.Unmarshal(buf, &amp;b)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Printf(<span class="string">&quot;%#v\n&quot;</span>, b) <span class="comment">// main.userInfo&#123;Name:&quot;wang&quot;, Age:18, Hobby:[]string&#123;&quot;Golang&quot;, &quot;TypeScript&quot;&#125;&#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="时间处理"><a href="#时间处理" class="headerlink" title="时间处理"></a>时间处理</h5><ul>
<li><p>在 Go 的标准库中，可以用 <code>time</code> 进行时间处理。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	now := time.Now()</span><br><span class="line">	fmt.Println(now) <span class="comment">// 2022-03-27 18:04:59.433297 +0800 CST m=+0.000087933</span></span><br><span class="line">	t := time.Date(<span class="number">2022</span>, <span class="number">3</span>, <span class="number">27</span>, <span class="number">1</span>, <span class="number">25</span>, <span class="number">36</span>, <span class="number">0</span>, time.UTC)</span><br><span class="line">	t2 := time.Date(<span class="number">2022</span>, <span class="number">3</span>, <span class="number">27</span>, <span class="number">2</span>, <span class="number">30</span>, <span class="number">36</span>, <span class="number">0</span>, time.UTC)</span><br><span class="line">	fmt.Println(t)                                                  <span class="comment">// 2022-03-27 01:25:36 +0000 UTC</span></span><br><span class="line">	fmt.Println(t.Year(), t.Month(), t.Day(), t.Hour(), t.Minute()) <span class="comment">// 2022 March 27 1 25</span></span><br><span class="line">	fmt.Println(t.Format(<span class="string">&quot;2006-01-02 15:04:05&quot;</span>))                    <span class="comment">// 2022-03-27 01:25:36</span></span><br><span class="line">	diff := t2.Sub(t)</span><br><span class="line">	fmt.Println(diff)                           <span class="comment">// 1h5m0s</span></span><br><span class="line">	fmt.Println(diff.Minutes(), diff.Seconds()) <span class="comment">// 65 3900</span></span><br><span class="line">	t3, err := time.Parse(<span class="string">&quot;2006-01-02 15:04:05&quot;</span>, <span class="string">&quot;2022-03-27 01:25:36&quot;</span>)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(t3 == t)    <span class="comment">// true</span></span><br><span class="line">	fmt.Println(now.Unix()) <span class="comment">// 1683795669</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="数字解析"><a href="#数字解析" class="headerlink" title="数字解析"></a>数字解析</h5><ul>
<li><p>在 Go 的标准库中，可以用 <code>strconv</code> 进行数字解析操作。 （<code>strconv</code> -&gt; <code>string convert</code>）</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;strconv&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	f, _ := strconv.ParseFloat(<span class="string">&quot;1.234&quot;</span>, <span class="number">64</span>)</span><br><span class="line">	fmt.Println(f) <span class="comment">// 1.234</span></span><br><span class="line"></span><br><span class="line">	n, _ := strconv.ParseInt(<span class="string">&quot;111&quot;</span>, <span class="number">10</span>, <span class="number">64</span>)</span><br><span class="line">	fmt.Println(n) <span class="comment">// 111</span></span><br><span class="line"></span><br><span class="line">	n, _ = strconv.ParseInt(<span class="string">&quot;0x1000&quot;</span>, <span class="number">0</span>, <span class="number">64</span>)</span><br><span class="line">	fmt.Println(n) <span class="comment">// 4096</span></span><br><span class="line"></span><br><span class="line">	n2, _ := strconv.Atoi(<span class="string">&quot;123&quot;</span>)</span><br><span class="line">	fmt.Println(n2) <span class="comment">// 123</span></span><br><span class="line"></span><br><span class="line">	n2, err := strconv.Atoi(<span class="string">&quot;AAA&quot;</span>)</span><br><span class="line">	fmt.Println(n2, err) <span class="comment">// 0 strconv.Atoi: parsing &quot;AAA&quot;: invalid syntax</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h5 id="进程信息"><a href="#进程信息" class="headerlink" title="进程信息"></a>进程信息</h5><ul>
<li><p>在 Go 的标准库中，可以用 <code>os</code> 进行进程操作。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;os&quot;</span></span><br><span class="line">	<span class="string">&quot;os/exec&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	<span class="comment">// go run example/20-env/main.go a b c d</span></span><br><span class="line">	fmt.Println(os.Args)           <span class="comment">// [/var/folders/8p/n34xxfnx38dg8bv_x8l62t_m0000gn/T/go-build3406981276/b001/exe/main a b c d]</span></span><br><span class="line">	fmt.Println(os.Getenv(<span class="string">&quot;PATH&quot;</span>)) <span class="comment">// /usr/local/go/bin...</span></span><br><span class="line">	fmt.Println(os.Setenv(<span class="string">&quot;AA&quot;</span>, <span class="string">&quot;BB&quot;</span>))</span><br><span class="line"></span><br><span class="line">	buf, err := exec.Command(<span class="string">&quot;grep&quot;</span>, <span class="string">&quot;127.0.0.1&quot;</span>, <span class="string">&quot;/etc/hosts&quot;</span>).CombinedOutput()</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Println(<span class="type">string</span>(buf)) <span class="comment">// 127.0.0.1       localhost</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="03-实战"><a href="#03-实战" class="headerlink" title="03 实战"></a>03 实战</h3><h4 id="3-1-猜谜游戏-生成随机数"><a href="#3-1-猜谜游戏-生成随机数" class="headerlink" title="3.1 猜谜游戏 - 生成随机数"></a>3.1 猜谜游戏 - 生成随机数</h4><ol>
<li><p>首先生成随机数，为了保证生成的随机数每次都不一样，需要使用随机种子：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	maxNum := <span class="number">100</span></span><br><span class="line">	rand.Seed(time.Now().UnixNano())</span><br><span class="line">	secretNumber := rand.Intn(maxNum)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后读取用户输入，这里使用的是 <code>bufio</code> 库来读取，并进行错误处理：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	reader := bufio.NewReader(os.Stdin)</span><br><span class="line">	input, err := reader.ReadString(<span class="string">&#x27;\n&#x27;</span>)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;An error occured while reading input. Please try again&quot;</span>, err)</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">	input = strings.Trim(input, <span class="string">&quot;\r\n&quot;</span>)</span><br><span class="line"></span><br><span class="line">	guess, err := strconv.Atoi(input)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;Invalid input. Please enter an integer value&quot;</span>)</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>接着对用户的输入进行判断，根据用户的猜测返回相应的回答：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> guess &gt; secretNumber &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;Your guess is bigger than the secret number. Please try again&quot;</span>)</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> guess &lt; secretNumber &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;Your guess is smaller than the secret number. Please try again&quot;</span>)</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		fmt.Println(<span class="string">&quot;Correct, you Legend!&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>最后在外面嵌套一层 <code>for</code> 循环，利用 <code>continue</code> 和 <code>break</code> 来控制流程的进行：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<ul>
<li>在这个例子中，涉及的知识点包括变量、循环、函数控制流、错误处理。</li>
</ul>
<h4 id="3-2-在线词典"><a href="#3-2-在线词典" class="headerlink" title="3.2 在线词典"></a>3.2 在线词典</h4><ol>
<li><p>利用彩云小译网站的词典库，来进行英语单词释义，首先在网站上得到其请求：</p>
<img src="../images/Go语言基础语法/彩云小译网络.png"  />
</li>
<li><p>再通过 <a href="https://curlconverter.com/go/">Convert curl to Go (curlconverter.com)</a> 来将请求转换为 Go 语言类型的程序：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">&quot;fmt&quot;</span></span><br><span class="line">	<span class="string">&quot;io&quot;</span></span><br><span class="line">	<span class="string">&quot;log&quot;</span></span><br><span class="line">	<span class="string">&quot;net/http&quot;</span></span><br><span class="line">	<span class="string">&quot;strings&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	client := &amp;http.Client&#123;&#125;</span><br><span class="line">	<span class="keyword">var</span> data = strings.NewReader(<span class="string">`&#123;&quot;trans_type&quot;:&quot;en2zh&quot;,&quot;source&quot;:&quot;awesome&quot;&#125;`</span>)</span><br><span class="line">	<span class="comment">//创建请求</span></span><br><span class="line">	req, err := http.NewRequest(<span class="string">&quot;POST&quot;</span>, <span class="string">&quot;https://api.interpreter.caiyunai.com/v1/dict&quot;</span>, data)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//设置请求头</span></span><br><span class="line">	req.Header.Set(<span class="string">&quot;authority&quot;</span>, <span class="string">&quot;api.interpreter.caiyunai.com&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;accept&quot;</span>, <span class="string">&quot;application/json, text/plain, */*&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;accept-language&quot;</span>, <span class="string">&quot;zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;app-name&quot;</span>, <span class="string">&quot;xy&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;content-type&quot;</span>, <span class="string">&quot;application/json;charset=UTF-8&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;device-id&quot;</span>, <span class="string">&quot;5b0ce53c39e131ad4014c3044e87886b&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;origin&quot;</span>, <span class="string">&quot;https://fanyi.caiyunapp.com&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;os-type&quot;</span>, <span class="string">&quot;web&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;os-version&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;referer&quot;</span>, <span class="string">&quot;https://fanyi.caiyunapp.com/&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-ch-ua&quot;</span>, <span class="string">`&quot;Microsoft Edge&quot;;v=&quot;113&quot;, &quot;Chromium&quot;;v=&quot;113&quot;, &quot;Not-A.Brand&quot;;v=&quot;24&quot;`</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-ch-ua-mobile&quot;</span>, <span class="string">&quot;?0&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-ch-ua-platform&quot;</span>, <span class="string">`&quot;Windows&quot;`</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-fetch-dest&quot;</span>, <span class="string">&quot;empty&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-fetch-mode&quot;</span>, <span class="string">&quot;cors&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;sec-fetch-site&quot;</span>, <span class="string">&quot;cross-site&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;user-agent&quot;</span>, <span class="string">&quot;Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/113.0.0.0 Safari/537.36 Edg/113.0.1774.35&quot;</span>)</span><br><span class="line">	req.Header.Set(<span class="string">&quot;x-authorization&quot;</span>, <span class="string">&quot;token:qgemv4jr1y38jyq6vhvi&quot;</span>)</span><br><span class="line">	<span class="comment">//发起请求</span></span><br><span class="line">	resp, err := client.Do(req)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">defer</span> resp.Body.Close()</span><br><span class="line">	<span class="comment">//读取响应</span></span><br><span class="line">	bodyText, err := io.ReadAll(resp.Body)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Printf(<span class="string">&quot;%s\n&quot;</span>, bodyText)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时返回的结果是 <code>JSON</code> 类型的字符串。</p>
</li>
<li><p>为了使得用户可以查找任意单词，需要将固定的 <code>JSON</code> 字符串输入转变为 <code>JSON</code> 序列化。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> DictRequest <span class="keyword">struct</span> &#123;</span><br><span class="line">	TransType <span class="type">string</span> <span class="string">`json:&quot;trans_type&quot;`</span></span><br><span class="line">	Source    <span class="type">string</span> <span class="string">`json:&quot;source&quot;`</span></span><br><span class="line">	UserID    <span class="type">string</span> <span class="string">`json:&quot;user_id&quot;`</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	client := &amp;http.Client&#123;&#125;</span><br><span class="line">	<span class="comment">//将固定的JSON字符串输入转变为JSON序列化</span></span><br><span class="line">	<span class="comment">// var data = strings.NewReader(`&#123;&quot;trans_type&quot;:&quot;en2zh&quot;,&quot;source&quot;:&quot;awesome&quot;&#125;`)</span></span><br><span class="line">	request := DictRequest&#123;TransType: <span class="string">&quot;en2zh&quot;</span>, Source: <span class="string">&quot;awesome&quot;</span>&#125;</span><br><span class="line">	buf, err := json.Marshal(request)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">var</span> data = bytes.NewReader(buf)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>由于返回的结果是 <code>JSON</code> 类型的字符串，难以阅读，所以需要对返回的结果进行处理。通过 <a href="https://oktools.net/json2go">JSON转Golang Struct - 在线工具 - OKTools</a> 来将返回的 <code>JSON</code> 字符串转换为对应的 <code>struct</code>，并通过 <code>json.Unmarshal</code> 方法来对返回的结果进行处理，并保存到结构体变量中：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">type</span> DictResponse <span class="keyword">struct</span> &#123;</span><br><span class="line">	Rc   <span class="type">int</span> <span class="string">`json:&quot;rc&quot;`</span></span><br><span class="line">	Wiki <span class="keyword">struct</span> &#123;</span><br><span class="line">	&#125; <span class="string">`json:&quot;wiki&quot;`</span></span><br><span class="line">	Dictionary <span class="keyword">struct</span> &#123;</span><br><span class="line">		Prons <span class="keyword">struct</span> &#123;</span><br><span class="line">			EnUs <span class="type">string</span> <span class="string">`json:&quot;en-us&quot;`</span></span><br><span class="line">			En   <span class="type">string</span> <span class="string">`json:&quot;en&quot;`</span></span><br><span class="line">		&#125; <span class="string">`json:&quot;prons&quot;`</span></span><br><span class="line">		Explanations []<span class="type">string</span>      <span class="string">`json:&quot;explanations&quot;`</span></span><br><span class="line">		Synonym      []<span class="keyword">interface</span>&#123;&#125; <span class="string">`json:&quot;synonym&quot;`</span></span><br><span class="line">		Antonym      []<span class="keyword">interface</span>&#123;&#125; <span class="string">`json:&quot;antonym&quot;`</span></span><br><span class="line">		WqxExample   []<span class="keyword">interface</span>&#123;&#125; <span class="string">`json:&quot;wqx_example&quot;`</span></span><br><span class="line">		Entry        <span class="type">string</span>        <span class="string">`json:&quot;entry&quot;`</span></span><br><span class="line">		Type         <span class="type">string</span>        <span class="string">`json:&quot;type&quot;`</span></span><br><span class="line">		Related      []<span class="keyword">interface</span>&#123;&#125; <span class="string">`json:&quot;related&quot;`</span></span><br><span class="line">		Source       <span class="type">string</span>        <span class="string">`json:&quot;source&quot;`</span></span><br><span class="line">	&#125; <span class="string">`json:&quot;dictionary&quot;`</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// fmt.Printf(&quot;%s\n&quot;, bodyText)</span></span><br><span class="line">	<span class="comment">//将响应结果转化并保存在结构体变量dictResponse中</span></span><br><span class="line">	<span class="keyword">var</span> dictResponse DictResponse</span><br><span class="line">	err = json.Unmarshal(bodyText, &amp;dictResponse)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	fmt.Printf(<span class="string">&quot;%#v\n&quot;</span>, dictResponse)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>但是现在结构体变量中的内容还是太多，我们需要进行处理来只输出我们想要的结果。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">//对输出进行裁剪选择</span></span><br><span class="line">	fmt.Println(<span class="string">&quot;awesome&quot;</span>, <span class="string">&quot;UK:&quot;</span>, dictResponse.Dictionary.Prons.En, <span class="string">&quot;US:&quot;</span>, dictResponse.Dictionary.Prons.EnUs)</span><br><span class="line">	<span class="keyword">for</span> _, item := <span class="keyword">range</span> dictResponse.Dictionary.Explanations &#123;</span><br><span class="line">		fmt.Println(item)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>最后将上面的代码封装为一个方法，传入参数为想要查询的单词即可。</p>
</li>
</ol>
<ul>
<li>在线词典需要注意的点有：<ol>
<li>将固定的 <code>JSON</code> 字符串输入转变为 <code>JSON</code> 序列化；</li>
<li>将返回的 <code>JSON</code> 字符串转换为对应的 <code>struct</code>；</li>
<li>从转化后的 <code>struct</code> 中得到我们真正想要的结果。</li>
</ol>
</li>
</ul>
<h4 id="3-3-SOCKS5-代理"><a href="#3-3-SOCKS5-代理" class="headerlink" title="3.3 SOCKS5 代理"></a>3.3 SOCKS5 代理</h4><ul>
<li><p>SOCKS5代理协议：SOCKS5协议是一款广泛使用的代理协议，它在使用 TCP&#x2F;IP 协议通讯的前端机器和服务器机器之间扮演一个中介角色，使得内部网中的前端机器变得能够访问 Internet 网中的服务器，或者使通讯更加安全。</p>
<p>SOCKS5 服务器通过将前端发来的请求转发给真正的目标服务器， 模拟了一个前端的行为。在这里，前端和SOCKS5之间也是通过TCP&#x2F;IP协议进行通讯，前端将原本要发送给真正服务器的请求发送给 SOCKS5 服务器，然后 SOCKS5 服务器将请求转发给真正的服务器。</p>
<p><img src="/../images/Go%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/SOCKS5.png"></p>
</li>
</ul>
<h5 id="工作原理"><a href="#工作原理" class="headerlink" title="工作原理"></a>工作原理</h5><ul>
<li><p>SOCKS5 通信的交互流程如下：</p>
<img src="../images/Go语言基础语法/SOCKS5原理.png" style="zoom:67%;" />

<p>可分为四个阶段：</p>
<ol>
<li><strong>协商（握手）阶段</strong>：客户端向代理服务器发送代理请求，其中包含了代理的版本和认证方式；</li>
<li><strong>认证阶段</strong>：服务端收到客户端的代理请求后，选择双方都支持的加密方式回复给客户端，此时客户端收到服务端的响应请求后，双方握手完成，开始进行协议交互；</li>
<li><strong>请求阶段</strong>：客户端向代理服务器发送请求，由代理服务器将请求转发给真正想要请求的服务器；</li>
<li><strong>relay阶段</strong>：客户端向代理服务器发送数据，由代理服务器将数据转发给服务器，并将服务器返回的响应结果发送给客户端。</li>
</ol>
</li>
<li><p>接下来就实现以上几个阶段。</p>
</li>
</ul>
<h5 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h5><ol>
<li><p>让服务器监听 <code>127.0.0.1:1080</code> 网址端口，如果收到客户端请求就进行处理：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	server, err := net.Listen(<span class="string">&quot;tcp&quot;</span>, <span class="string">&quot;127.0.0.1:1080&quot;</span>)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="built_in">panic</span>(err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">for</span> &#123;</span><br><span class="line">		client, err := server.Accept()</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">			log.Printf(<span class="string">&quot;Accept failed %v&quot;</span>, err)</span><br><span class="line">			<span class="keyword">continue</span></span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">go</span> process(client)</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>客户端和代理服务器之间的<strong>协商认证阶段</strong>，客户端向代理服务器发送类似注释中的数据，代理服务器对客户端发送的数据进行错误处理，如果没有错误，就返回认证信息：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">auth</span><span class="params">(reader *bufio.Reader, conn net.Conn)</span></span> (err <span class="type">error</span>) &#123;</span><br><span class="line">	<span class="comment">// +----+----------+----------+</span></span><br><span class="line">	<span class="comment">// |VER | NMETHODS | METHODS  |</span></span><br><span class="line">	<span class="comment">// +----+----------+----------+</span></span><br><span class="line">	<span class="comment">// | 1  |    1     | 1 to 255 |</span></span><br><span class="line">	<span class="comment">// +----+----------+----------+</span></span><br><span class="line">	<span class="comment">// VER: 协议版本，socks5为0x05</span></span><br><span class="line">	<span class="comment">// NMETHODS: 支持认证的方法数量</span></span><br><span class="line">	<span class="comment">// METHODS: 对应NMETHODS，NMETHODS的值为多少，METHODS就有多少个字节。RFC预定义了一些值的含义，内容如下:</span></span><br><span class="line">	<span class="comment">// X’00’ NO AUTHENTICATION REQUIRED</span></span><br><span class="line">	<span class="comment">// X’02’ USERNAME/PASSWORD</span></span><br><span class="line"></span><br><span class="line">	ver, err := reader.ReadByte()</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read ver failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ver != socks5Ver &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;not supported ver:%v&quot;</span>, ver)</span><br><span class="line">	&#125;</span><br><span class="line">	methodSize, err := reader.ReadByte()</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read methodSize failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	method := <span class="built_in">make</span>([]<span class="type">byte</span>, methodSize)</span><br><span class="line">	_, err = io.ReadFull(reader, method)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read method failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	log.Println(<span class="string">&quot;ver&quot;</span>, ver, <span class="string">&quot;method&quot;</span>, method)</span><br><span class="line">	<span class="comment">// +----+--------+</span></span><br><span class="line">	<span class="comment">// |VER | METHOD |</span></span><br><span class="line">	<span class="comment">// +----+--------+</span></span><br><span class="line">	<span class="comment">// | 1  |   1    |</span></span><br><span class="line">	<span class="comment">// +----+--------+</span></span><br><span class="line">	_, err = conn.Write([]<span class="type">byte</span>&#123;socks5Ver, <span class="number">0x00</span>&#125;)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;write failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>请求阶段</strong>。在客户端和代理服务器之间建立了连接后，客户端就向代理服务器发送请求，由代理服务器将请求转发给真正想要请求的服务器，再由服务器返回响应：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">connect</span><span class="params">(reader *bufio.Reader, conn net.Conn)</span></span> (err <span class="type">error</span>) &#123;</span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// |VER | CMD |  RSV  | ATYP | DST.ADDR | DST.PORT |</span></span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// | 1  |  1  | X&#x27;00&#x27; |  1   | Variable |    2     |</span></span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// VER 版本号，socks5的值为0x05</span></span><br><span class="line">	<span class="comment">// CMD 0x01表示CONNECT请求</span></span><br><span class="line">	<span class="comment">// RSV 保留字段，值为0x00</span></span><br><span class="line">	<span class="comment">// ATYP 目标地址类型，DST.ADDR的数据对应这个字段的类型。</span></span><br><span class="line">	<span class="comment">//   0x01表示IPv4地址，DST.ADDR为4个字节</span></span><br><span class="line">	<span class="comment">//   0x03表示域名，DST.ADDR是一个可变长度的域名</span></span><br><span class="line">	<span class="comment">// DST.ADDR 一个可变长度的值</span></span><br><span class="line">	<span class="comment">// DST.PORT 目标端口，固定2个字节</span></span><br><span class="line"></span><br><span class="line">	buf := <span class="built_in">make</span>([]<span class="type">byte</span>, <span class="number">4</span>)            <span class="comment">//四个字节的缓冲区</span></span><br><span class="line">	_, err = io.ReadFull(reader, buf) <span class="comment">//先填充满buf,内容包括VER、CMD、RSV、ATYP</span></span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read header failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	ver, cmd, atyp := buf[<span class="number">0</span>], buf[<span class="number">1</span>], buf[<span class="number">3</span>]</span><br><span class="line">	<span class="comment">//对VER、CMD、ATYP进行错误处理</span></span><br><span class="line">	<span class="keyword">if</span> ver != socks5Ver &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;not supported ver:%v&quot;</span>, ver)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> cmd != cmdBind &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;not supported cmd:%v&quot;</span>, ver)</span><br><span class="line">	&#125;</span><br><span class="line">	addr := <span class="string">&quot;&quot;</span></span><br><span class="line">	<span class="keyword">switch</span> atyp &#123;</span><br><span class="line">	<span class="comment">//根据ip地址的不同类型进行相应的处理</span></span><br><span class="line">	<span class="keyword">case</span> atypIPV4:</span><br><span class="line">		_, err = io.ReadFull(reader, buf)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read atyp failed:%w&quot;</span>, err)</span><br><span class="line">		&#125;</span><br><span class="line">		addr = fmt.Sprintf(<span class="string">&quot;%d.%d.%d.%d&quot;</span>, buf[<span class="number">0</span>], buf[<span class="number">1</span>], buf[<span class="number">2</span>], buf[<span class="number">3</span>])</span><br><span class="line">	<span class="keyword">case</span> atypeHOST:</span><br><span class="line">		hostSize, err := reader.ReadByte()</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read hostSize failed:%w&quot;</span>, err)</span><br><span class="line">		&#125;</span><br><span class="line">		host := <span class="built_in">make</span>([]<span class="type">byte</span>, hostSize)</span><br><span class="line">		_, err = io.ReadFull(reader, host)</span><br><span class="line">		<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">			<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read host failed:%w&quot;</span>, err)</span><br><span class="line">		&#125;</span><br><span class="line">		addr = <span class="type">string</span>(host)</span><br><span class="line">	<span class="keyword">case</span> atypeIPV6:</span><br><span class="line">		<span class="keyword">return</span> errors.New(<span class="string">&quot;IPv6: no supported yet&quot;</span>)</span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">		<span class="keyword">return</span> errors.New(<span class="string">&quot;invalid atyp&quot;</span>)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//读取端口号保存在buf中</span></span><br><span class="line">	_, err = io.ReadFull(reader, buf[:<span class="number">2</span>])</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;read port failed:%w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	port := binary.BigEndian.Uint16(buf[:<span class="number">2</span>])</span><br><span class="line"></span><br><span class="line">	log.Println(<span class="string">&quot;dial&quot;</span>, addr, port)</span><br><span class="line"></span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// |VER | REP |  RSV  | ATYP | BND.ADDR | BND.PORT |</span></span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// | 1  |  1  | X&#x27;00&#x27; |  1   | Variable |    2     |</span></span><br><span class="line">	<span class="comment">// +----+-----+-------+------+----------+----------+</span></span><br><span class="line">	<span class="comment">// VER socks版本，这里为0x05</span></span><br><span class="line">	<span class="comment">// REP Relay field,内容取值如下 X’00’ succeeded</span></span><br><span class="line">	<span class="comment">// RSV 保留字段</span></span><br><span class="line">	<span class="comment">// ATYPE 地址类型</span></span><br><span class="line">	<span class="comment">// BND.ADDR 服务绑定的地址</span></span><br><span class="line">	<span class="comment">// BND.PORT 服务绑定的端口DST.PORT</span></span><br><span class="line">	<span class="comment">//服务器返回响应</span></span><br><span class="line">	_, err = conn.Write([]<span class="type">byte</span>&#123;<span class="number">0x05</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x01</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>&#125;)</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> fmt.Errorf(<span class="string">&quot;write failed: %w&quot;</span>, err)</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>relay阶段</strong>：在请求阶段的代码中，添加服务器对客户端请求的数据的响应结果：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">connect</span><span class="params">(reader *bufio.Reader, conn net.Conn)</span></span> (err <span class="type">error</span>) &#123;</span><br><span class="line">    </span><br><span class="line">	...	<span class="comment">//请求阶段代码</span></span><br><span class="line">    </span><br><span class="line">	ctx, cancel := context.WithCancel(context.Background())</span><br><span class="line">	<span class="keyword">defer</span> cancel()</span><br><span class="line"></span><br><span class="line">	<span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">		_, _ = io.Copy(dest, reader)</span><br><span class="line">		cancel()</span><br><span class="line">	&#125;()</span><br><span class="line">	<span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">		_, _ = io.Copy(conn, dest)</span><br><span class="line">		cancel()</span><br><span class="line">	&#125;()</span><br><span class="line"></span><br><span class="line">	&lt;-ctx.Done()</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
]]></content>
      <categories>
        <category>ByteDance-BackEnd-notebook</category>
      </categories>
  </entry>
</search>
